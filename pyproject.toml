[project]
name = "statcast-spray-angle-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pybaseball>=2.2.7",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
